
- Why do we need to set LD_LIBRARY_PATH?
- Move declarations to .pxd file
- Automatic generation of the .pxd file (e.g. with xdress?)
- Write manually some minimal Python wrapper
- Automatic generation of the Python wrappers?
