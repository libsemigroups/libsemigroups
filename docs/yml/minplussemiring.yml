libsemigroups::MinPlusSemiring:
- _kind: struct
- Constructors:
  - MinPlusSemiring()
  - MinPlusSemiring(MinPlusSemiring const&)
  - MinPlusSemiring(MinPlusSemiring&&)
  - operator=(MinPlusSemiring const&)
  - operator=(MinPlusSemiring&&)
- Public member functions:
  - one
  - zero
  - plus
  - prod
  - contains
