libsemigroups::CongruenceByPairs:
- Aliases:
  - [typedef, element_type]
  - [typedef, const_reference]
  - [typedef, class_index_type]
  - [typedef, non_trivial_classes_type]
  - [typedef, non_trivial_class_iterator]
  - [typedef, const_iterator]
- Constructors:
  - CongruenceByPairs
  - CongruenceByPairs(CongruenceByPairs const&)
  - CongruenceByPairs(congruence_type, std::shared_ptr<FroidurePinBase>)
  - CongruenceByPairs(congruence_type, T const&)
  - operator=(CongruenceByPairs const&)
  - CongruenceByPairs(CongruenceByPairs&&)
  - operator=(CongruenceByPairs&&)
- Initialization:
  - set_nr_generators
  - nr_generators
  - add_pair(std::initializer_list<size_t>, std::initializer_list<size_t>)
  - add_pair(word_type const&, word_type const&)
- Reporting:
  - report_every(TIntType)
  - report_every(std::chrono::nanoseconds)
  - report
  - report_why_we_stopped
- Operators:
  - kill
  - run
  - run_for(std::chrono::nanoseconds)
  - run_for(TIntType)
  - run_until
- Container-like:
  - less
  - const_contains
  - contains
  - nr_classes
  - nr_non_trivial_classes
  - non_trivial_classes
  - nr_generating_pairs
- Related semigroups:
  - quotient_froidure_pin
  - has_quotient_froidure_pin
  - parent_froidure_pin
  - has_parent_froidure_pin
  - is_quotient_obviously_finite
  - is_quotient_obviously_infinite
- Words and class indices:
  - word_to_class_index
  - class_index_to_word
- State:
  - kind
  - dead
  - finished
  - started
  - stopped
  - timed_out
  - running
  - stopped_by_predicate
- Iterators:
  - cbegin_generating_pairs
  - cend_generating_pairs
  - cbegin_ntc
  - cend_ntc
