---
brief: True
details: True
implements: True
url_source: True
---

{# This template is used inside of member.py #}
{% filter indent(config.get('indent_level', 0), True) %}

<dl class="cpp function">
<dt class="sig sig-object cpp" id="xxx">
{{node.name_long if node.is_group else node.name_short.replace("\\", "")}}
{{node.overload_suffix}}
</dt> 
</dl>
<a name="{{node.kind.value}}-{{node.name_long.replace("\\", "") if node.is_group else node.name_short.replace("\\", "")}}"></a>

{% if not node.is_typedef %}
```{{node.code_language}}
{{node.codeblock.replace("\n", "")}}
```
{% endif %}

{{node.brief + "\n"}}

{% if configMemDef.get('details') -%}
{% if node.has_details -%}
{{node.details}}
{%- endif -%}
{%- endif -%}

{% if configMemDef.get('implements') -%}
{% if node.reimplements %}
Implements [*{{node.reimplements.name_long}}*]({{node.reimplements.url}})
{% endif %}
{%- endif -%}

{% endfilter %}
